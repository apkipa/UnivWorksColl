* 这是交互式控制台。输入命令来操作 B-树。
(N/A)> n s 3
* 正在创建名为 `s` 的新 B-树(度=3)...
(N/A)> s s
(s)> i 3
* 向 `s` 中插入了 `3`。
(s)> i 7
* 向 `s` 中插入了 `7`。
(s)> i 5
* 向 `s` 中插入了 `5`。
(s)> dump
{"order":3,"root":{"children":[{"children":[],"keys":[3]},{"children":[],"keys":[7]}],"keys":[5]}}
(s)> i 1
* 向 `s` 中插入了 `1`。
(s)> i 2
* 向 `s` 中插入了 `2`。
(s)> dump
{"order":3,"root":{"children":[{"children":[],"keys":[1]},{"children":[],"keys":[3]},{"children":[],"keys":[7]}],"keys":[2,5]}}
(s)> unload s
错误: 执行命令时抛出了异常: 命令不接受 1 个参数(预期 0 个)
(s)> unload
(N/A)> n s 6
* 正在创建名为 `s` 的新 B-树(度=6)...
(N/A)> s s
(s)> i 57
* 向 `s` 中插入了 `57`。
(s)> i 39
* 向 `s` 中插入了 `39`。
(s)> i 74
* 向 `s` 中插入了 `74`。
(s)> i 90
* 向 `s` 中插入了 `90`。
(s)> i 60
* 向 `s` 中插入了 `60`。
(s)> i 8
* 向 `s` 中插入了 `8`。
(s)> i 65
* 向 `s` 中插入了 `65`。
(s)> i 88
* 向 `s` 中插入了 `88`。
(s)> i 5
* 向 `s` 中插入了 `5`。
(s)> i 85
* 向 `s` 中插入了 `85`。
(s)> i 4
* 向 `s` 中插入了 `4`。
(s)> i 92
* 向 `s` 中插入了 `92`。
(s)> i 36
* 向 `s` 中插入了 `36`。
(s)> i 38
* 向 `s` 中插入了 `38`。
(s)> i 29
* 向 `s` 中插入了 `29`。
(s)> i 97
* 向 `s` 中插入了 `97`。
(s)> i 49
* 向 `s` 中插入了 `49`。
(s)> i 82
* 向 `s` 中插入了 `82`。
(s)> i 80
* 向 `s` 中插入了 `80`。
(s)> i 87
* 向 `s` 中插入了 `87`。
(s)> i 9
* 向 `s` 中插入了 `9`。
(s)> i 7
* 向 `s` 中插入了 `7`。
(s)> i 61
* 向 `s` 中插入了 `61`。
(s)> i 99
* 向 `s` 中插入了 `99`。
(s)> i 13
* 向 `s` 中插入了 `13`。
(s)> i 6
* 向 `s` 中插入了 `6`。
(s)> i 72
* 向 `s` 中插入了 `72`。
(s)> i 81
* 向 `s` 中插入了 `81`。
(s)> i 28
* 向 `s` 中插入了 `28`。
(s)> i 98
* 向 `s` 中插入了 `98`。
(s)> i 1
* 向 `s` 中插入了 `1`。
(s)> i 34
* 向 `s` 中插入了 `34`。
(s)> i 30
* 向 `s` 中插入了 `30`。
(s)> i 64
* 向 `s` 中插入了 `64`。
(s)> i 56
* 向 `s` 中插入了 `56`。
(s)> i 94
* 向 `s` 中插入了 `94`。
(s)> i 95
* 向 `s` 中插入了 `95`。
(s)> i 21
* 向 `s` 中插入了 `21`。
(s)> i 17
* 向 `s` 中插入了 `17`。
(s)> i 83
* 向 `s` 中插入了 `83`。
(s)> i 26
* 向 `s` 中插入了 `26`。
(s)> i 89
* 向 `s` 中插入了 `89`。
(s)> i 48
* 向 `s` 中插入了 `48`。
(s)> i 70
* 向 `s` 中插入了 `70`。
(s)> i 18
* 向 `s` 中插入了 `18`。
(s)> i 69
* 向 `s` 中插入了 `69`。
(s)> i 52
* 向 `s` 中插入了 `52`。
(s)> i 100
* 向 `s` 中插入了 `100`。
(s)> i 12
* 向 `s` 中插入了 `12`。
(s)> i 78
* 向 `s` 中插入了 `78`。
(s)> dump
{"order":6,"root":{"children":[{"children":[{"children":[],"keys":[1,4,5,6,7]},{"children":[],"keys":[9,12,13]},{"children":[],"keys":[18,21,26]},{"children":[],"keys":[29,30,34]}],"keys":[8,17,28]},{"children":[{"children":[],"keys":[38,39]},{"children":[],"keys":[49,52,56]},{"children":[],"keys":[60,61]},{"children":[],"keys":[65,69,70,72]}],"keys":[48,57,64]},{"children":[{"children":[],"keys":[78,80,81]},{"children":[],"keys":[83,85,87]},{"children":[],"keys":[89,90,92]},{"children":[],"keys":[95,97,98,99,100]}],"keys":[82,88,94]}],"keys":[36,74]}}
(s)> sync btree.txt
* 正在向 `btree.txt` 写入名为 `s` 的 B-树数据...
(s)> unload
(N/A)> load s btree.txt
* 正在从 `btree.txt` 读取名为 `s` 的 B-树数据...
(N/A)> s s
(s)> dump
{"order":6,"root":{"children":[{"children":[{"children":[],"keys":[1,4,5,6,7]},{"children":[],"keys":[9,12,13]},{"children":[],"keys":[18,21,26]},{"children":[],"keys":[29,30,34]}],"keys":[8,17,28]},{"children":[{"children":[],"keys":[38,39]},{"children":[],"keys":[49,52,56]},{"children":[],"keys":[60,61]},{"children":[],"keys":[65,69,70,72]}],"keys":[48,57,64]},{"children":[{"children":[],"keys":[78,80,81]},{"children":[],"keys":[83,85,87]},{"children":[],"keys":[89,90,92]},{"children":[],"keys":[95,97,98,99,100]}],"keys":[82,88,94]}],"keys":[36,74]}}
(s)> d 30
* 从 `s` 中删除了 `30`。
(s)> d 36
* 从 `s` 中删除了 `36`。
(s)> d 100
* 从 `s` 中删除了 `100`。
(s)> dump
{"order":6,"root":{"children":[{"children":[{"children":[],"keys":[1,4,5,6,7]},{"children":[],"keys":[9,12,13]},{"children":[],"keys":[18,21]},{"children":[],"keys":[28,29]}],"keys":[8,17,26]},{"children":[{"children":[],"keys":[38,39]},{"children":[],"keys":[49,52,56]},{"children":[],"keys":[60,61]},{"children":[],"keys":[65,69,70,72]}],"keys":[48,57,64]},{"children":[{"children":[],"keys":[78,80,81]},{"children":[],"keys":[83,85,87]},{"children":[],"keys":[89,90,92]},{"children":[],"keys":[95,97,98,99]}],"keys":[82,88,94]}],"keys":[34,74]}}